import 'package:flutter/material.dart';

class TopClearance extends StatelessWidget {
  const TopClearance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      color: Colors.white,
      child: const SizedBox(height: 110),
    );
  }
}
